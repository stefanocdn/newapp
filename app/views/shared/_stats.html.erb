<% @user ||= current_user %>
<div class="stats">
  <a href="<%= group_user_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.groups.count %>
    </strong>
    groups
  </a>
</div>